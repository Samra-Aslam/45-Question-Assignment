var Users = ["samra", "saba", "samavia", "sonia"];
if (Users.length === 0) {
    console.log(" we need to find some Users");
}
else {
    console.log(" we need to enter new users names ");
}
//Remove all Users name from array
Users = [];
if (Users.length === 0) {
    console.log(" we need to find some Users");
}
else {
    console.log(" we need to enter new users neame ");
}
